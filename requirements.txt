# 异步 HTTP 请求库（用于翻译 API）
aiohttp>=3.13.0

# 阿里云 DashScope SDK（用于语音识别）
dashscope>=1.24.0,<1.25.0

# 音频处理库
pyaudio>=0.2.13

# OSC 通信库（用于 VRChat 通信）
python-osc>=1.8.0

# 环境变量管理
python-dotenv>=1.0.0

# 翻译库
googletrans>=4.0.0
deepl
openai>=2.8.0
httpx>=0.27.0
pykakasi>=2.2.1
pypinyin>=0.51.0
jieba>=0.42.1

# 语言检测库（用于自动检测源语言）
fast-langdetect>=0.2.0

# WebSocket 客户端（用于部分 ASR/流式组件）
websockets>=12.0

# Web UI 框架
flask>=3.0.0
flask-cors>=4.0.0
